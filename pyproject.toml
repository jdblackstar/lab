[project]
name = "lab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "verifiers>=0.1.11",
]

[dependency-groups]
dev = [
    "ruff>=0.15.8",
]

[tool.ruff]
extend-exclude = ["external"]
